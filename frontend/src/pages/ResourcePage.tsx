import {useEffect, useState} from 'react';
import {BookOpen} from 'lucide-react';
import {api} from '../services/api';
import {apiErrorMessage} from '../utils/apiError';
import {Card, Empty, PageHeader, Skeleton} from '../components/UI';

export default function ResourcePage({title, endpoint}: {title: string; endpoint: string}) {const [rows, setRows] = useState<Record<string, unknown>[] | null>(null); const [error, setError] = useState(''); useEffect(() => {let active = true; api.get(endpoint).then(response => {if (active) setRows(response.data.results || response.data);}).catch(reason => {if (active) {setError(apiErrorMessage(reason, `Unable to load ${title.toLowerCase()}.`)); setRows([]);}}); return () => {active = false;};}, [endpoint, title]); return <><PageHeader eyebrow="Learning records" title={title} description={`Review and manage ${title.toLowerCase()} for the students available to you.`}/><Card>{error && <div className="alert" role="alert">{error}</div>}{!rows ? <Skeleton rows={7}/> : !rows.length ? <Empty icon={BookOpen} text={`No ${title.toLowerCase()} found`} description="Learning records will appear here once progress is recorded."/> : <div className="table-wrap"><table><thead><tr>{Object.keys(rows[0]).filter(key => !['created_at', 'updated_at'].includes(key)).slice(0, 6).map(key => <th key={key}>{key.replaceAll('_', ' ')}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={String(row.id || index)}>{Object.entries(row).filter(([key]) => !['created_at', 'updated_at'].includes(key)).slice(0, 6).map(([key, value]) => <td key={key}>{typeof value === 'object' ? JSON.stringify(value) : String(value ?? '—')}</td>)}</tr>)}</tbody></table></div>}</Card></>;}
