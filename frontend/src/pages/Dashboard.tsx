import {useEffect,useState} from 'react'; import {Users,UserCheck,UserX,GraduationCap} from 'lucide-react'; import {api} from '../services/api'; import {useAuth} from '../context/AuthContext'; import {Card,Spinner} from '../components/UI';
type Data={students:{total:number;active:number;inactive:number};teachers?:number;attendance_percentage:number;today_attendance:Record<string,number>;recent_hifz:{id:number;student__first_name:string;student__last_name:string;surah:number;progress_percentage:number;status:string}[]};
export default function Dashboard(){const {user}=useAuth(),[data,setData]=useState<Data|null>(null);useEffect(()=>{api.get(`/dashboard/${user?.role}/`).then(r=>setData(r.data))},[user]);if(!data)return <Spinner/>;const cards=[['Students',data.students.total,Users],['Active',data.students.active,UserCheck],['Inactive',data.students.inactive,UserX],...(user?.role==='admin'?[['Teachers',data.teachers||0,GraduationCap] as const]:[])];return <><div className="page-title"><div><h1>Assalamu alaikum, {user?.first_name}</h1><p>Here is today’s institution overview.</p></div></div><div className="stats">{cards.map(([label,value,Icon])=><Card key={label}><Icon/><span>{label}</span><strong>{value}</strong></Card>)}</div><div className="grid-2"><Card><h2>Today’s attendance</h2><div className="attendance-ring"><strong>{data.attendance_percentage}%</strong><span>Present or late</span></div><div className="legend">{['present','absent','late','excused'].map(s=><span key={s}><i className={s}/>{s} <b>{data.today_attendance[s]||0}</b></span>)}</div></Card><Card><h2>Recent Hifz updates</h2>{data.recent_hifz.length?data.recent_hifz.map(x=><div className="activity" key={x.id}><div><strong>{x.student__first_name} {x.student__last_name}</strong><small>Surah {x.surah} · {x.status.replace('_',' ')}</small></div><b>{x.progress_percentage}%</b></div>):<p className="muted">No recent updates.</p>}</Card></div></>}
