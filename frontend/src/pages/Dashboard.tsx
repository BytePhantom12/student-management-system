import {useEffect,useState,type CSSProperties} from 'react';
import {GraduationCap,UserCheck,UserX,Users} from 'lucide-react';
import {api} from '../services/api';import {apiErrorMessage} from '../utils/apiError';import {useAuth} from '../context/auth';import {Card,Empty,PageHeader,Skeleton,StatCard} from '../components/UI';import Feedback from '../components/Feedback';import type {DashboardData} from '../types';

export default function Dashboard(){const {user}=useAuth();const [data,setData]=useState<DashboardData|null>(null);const [error,setError]=useState('');useEffect(()=>{api.get<DashboardData>(`/dashboard/${user?.is_admin?'admin':'teacher'}/`).then(response=>setData(response.data)).catch(reason=>setError(apiErrorMessage(reason,'Unable to load the dashboard.')));},[user]);if(error&&!data)return <Feedback tone="error" message={error}/>;if(!data)return <><PageHeader eyebrow="Institution overview" title="Dashboard" description="Loading your latest Student and learning information."/><Skeleton rows={4} cards/></>;const attendanceStyle={'--attendance':`${Math.min(100,data.attendance_percentage)}%`} as CSSProperties;
  return <><PageHeader eyebrow="Institution overview" title={<>Assalamu alaikum, {user?.first_name||user?.username}</>} description="A clear view of today’s Students, attendance, and recent learning activity."/><div className="stats"><StatCard label="Total Students" value={data.students.total} icon={Users} supporting={`${data.students.active} currently active`}/><StatCard label="Active Students" value={data.students.active} icon={UserCheck}/><StatCard label="Inactive Students" value={data.students.inactive} icon={UserX} tone="red"/>{user?.is_admin&&<StatCard label="Active Teachers" value={data.teachers||0} icon={GraduationCap} tone="gold"/>}</div><div className="grid-2"><Card><h2>Today’s attendance</h2><div className="attendance-ring" style={attendanceStyle}><strong>{data.attendance_percentage}%</strong><span>Present or late</span></div><div className="legend">{['present','absent','late','excused'].map(status=><span key={status}><i className={status}/>{status} <b>{data.today_attendance[status]||0}</b></span>)}</div></Card><Card><h2>Recent Hifz updates</h2>{data.recent_hifz.length?data.recent_hifz.map(item=><div className="activity" key={item.id}><div><strong>{item.student__first_name} {item.student__last_name}</strong><small>Surah {item.surah} · {item.status.replaceAll('_',' ')}</small></div><b>{item.progress_percentage}%</b></div>):<Empty text="No recent Hifz updates" description="New progress records will appear here."/>}</Card></div>{user?.is_admin&&<Card><h2>Active Students by Teacher</h2>{data.students_by_teacher?.length?<div className="rank-list">{[...data.students_by_teacher].sort((a,b)=>b.count-a.count).map(item=>{const name=`${item.user__first_name} ${item.user__last_name}`.trim()||`Teacher #${item.id}`;const maximum=Math.max(...data.students_by_teacher!.map(row=>row.count),1);return <div key={item.id}><span><strong>{name}</strong><b>{item.count}</b></span><i><em style={{width:`${item.count*100/maximum}%`}}/></i></div>;})}</div>:<Empty text="No Teacher assignments" description="Active Student assignments will appear here."/>}</Card>}</>;
}
