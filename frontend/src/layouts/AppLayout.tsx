import {BookOpen,LayoutDashboard,Users,UserRound,CalendarCheck,LogOut,Menu,X,ShieldCheck,ClipboardList} from 'lucide-react'; import type {LucideIcon} from 'lucide-react'; import {NavLink,Outlet} from 'react-router-dom'; import {useState} from 'react'; import {useAuth} from '../context/auth';
export default function AppLayout(){const {user,logout}=useAuth(),[open,setOpen]=useState(false);const links: [string,string,LucideIcon][]=[['/dashboard','Dashboard',LayoutDashboard],['/students','Students',Users],['/hifz','Hifz Progress',BookOpen],['/attendance','Attendance',CalendarCheck],...(user?.is_admin?[['/teachers','Teachers',UserRound] as [string,string,LucideIcon],['/guardians','Parents / Guardians',Users] as [string,string,LucideIcon]]:[])];const superuserLinks:[string,string,LucideIcon][]=[['/superuser','Overview',ShieldCheck],['/superuser/users','Users',UserRound],['/superuser/audit','Audit Logs',ClipboardList]];return <div className="shell"><aside className={open?'open':''}><div className="brand"><BookOpen/><div><strong>Halqatu Darul Taqwa</strong><small>Student Management</small></div><button className="mobile-close" onClick={()=>setOpen(false)}><X/></button></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}><Icon size={19}/>{label}</NavLink>)}{user?.is_superuser&&<><span className="nav-section">Superuser</span>{superuserLinks.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/superuser'} onClick={()=>setOpen(false)}><Icon size={19}/>{label}</NavLink>)}</>}</nav><button className="logout" onClick={logout}><LogOut size={18}/> Sign out</button></aside><main><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div><strong>{user?.first_name} {user?.last_name}</strong><small>{user?.is_superuser?'superuser':user?.role}</small></div></header><div className="content"><Outlet/></div></main>{open&&<div className="scrim" onClick={()=>setOpen(false)}/>}</div>}
