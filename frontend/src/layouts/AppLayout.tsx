import {BookOpen,LayoutDashboard,Users,UserRound,CalendarCheck,LogOut,Menu,X} from 'lucide-react'; import {NavLink,Outlet} from 'react-router-dom'; import {useState} from 'react'; import {useAuth} from '../context/AuthContext';
export default function AppLayout(){const {user,logout}=useAuth(),[open,setOpen]=useState(false);const links=[['/dashboard','Dashboard',LayoutDashboard],['/students','Students',Users],['/hifz','Hifz Progress',BookOpen],['/attendance','Attendance',CalendarCheck],...(user?.role==='admin'?[['/teachers','Teachers',UserRound] as const]:[])];return <div className="shell"><aside className={open?'open':''}><div className="brand"><BookOpen/><div><strong>Darul Taqwa</strong><small>Student Management</small></div><button className="mobile-close" onClick={()=>setOpen(false)}><X/></button></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}><Icon size={19}/>{label}</NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={18}/> Sign out</button></aside><main><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div><strong>{user?.first_name} {user?.last_name}</strong><small>{user?.role}</small></div></header><div className="content"><Outlet/></div></main>{open&&<div className="scrim" onClick={()=>setOpen(false)}/>}</div>}
