import {Navigate,Route,Routes,useLocation} from 'react-router-dom'; import {useAuth} from './context/AuthContext'; import AppLayout from './layouts/AppLayout'; import Login from './pages/Login'; import Dashboard from './pages/Dashboard'; import Students from './pages/Students'; import StudentForm from './pages/StudentForm'; import StudentProfile from './pages/StudentProfile'; import ResourcePage from './pages/ResourcePage'; import {Spinner} from './components/UI';
function Protected(){const {user,loading}=useAuth(),location=useLocation();if(loading)return <Spinner/>;return user?<AppLayout/>:<Navigate to="/login" state={{from:location.pathname}} replace/>}export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route element={<Protected/>}><Route path="/dashboard" element={<Dashboard/>}/><Route path="/students" element={<Students/>}/><Route path="/students/new" element={<StudentForm/>}/><Route path="/students/:id" element={<StudentProfile/>}/><Route path="/students/:id/edit" element={<StudentForm/>}/><Route path="/hifz" element={<ResourcePage title="Hifz Progress" endpoint="/hifz/"/>}/><Route path="/attendance" element={<ResourcePage title="Attendance" endpoint="/attendance/"/>}/><Route path="/teachers" element={<ResourcePage title="Teachers" endpoint="/teachers/"/>}/></Route><Route path="*" element={<Navigate to="/dashboard"/>}/></Routes>}

